// TODO: args: title: string, slug?: string
// create new markdown file in content/posts with the name based on a sluggified title or the slug if passed
function createPost() {
  console.log("run");
}

createPost();
